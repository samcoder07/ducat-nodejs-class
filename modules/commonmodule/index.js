const {items:i} = require('./items')

const printItems = require('./printItems').default

console.log(printItems);

printItems(i)

// note: in whole file should be single it does not support multiple 