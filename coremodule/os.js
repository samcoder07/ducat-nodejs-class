// the node: os module provides operating system-related 
// utility method and properties 
// freemen() return total free memory in RAM 
// it returns total free bytes in main memory 

// console.log(os.freemen()/1024/1024/1024);

// totalmen() return total memory in RAM
// it returns total bytes in main memory

// console.log(os.totalmen()/1024/1024/1024);

// os.homedir() return home directory of user 

// console.log(os.homedir());

// os.platform() return plateform of os 

// console.log(os.platform());

// os.arch() return architecture of os 

// console.log(os.arch());

// os.uptime() return time from windows os 

// console.log(os.uptime()/60/60);

// os.userInfo() return user information 

// console.log(os.userInfo());

// os.version() return version of os 

// console.log(os.version());

// os.cpus() return information about processor 

// console.log(os.cpus());

// os.release() return os release version

// console.log(os.release());

// os.type() return the os name by type 

// console.log(os.type());